# Shell scripts must stay LF on every checkout. Windows autocrlf=true would
# otherwise rewrite them to CRLF, which breaks the bash shebang on the VPS
# and trips guardian-wrapper drift detection on Windows-side checkouts.
*.sh text eol=lf
